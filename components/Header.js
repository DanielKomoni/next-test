import React from "react";
import HStyle from "../styles/Header.module.css"

const Header=()=>{
    return(
        <div>
            <h1 className={HStyle.a}>Header</h1>
        </div>
    )
}

export default Header;